import { } from './actions';

const INITIAL_STATE = {

};

const reducer = (state = INITIAL_STATE, action: any) => {
    switch (action.type) {
        // case :

        default:
            return state;
    }
};

export default reducer;
